#signup-box
  %h3 Learn more about the 8-week Body Test System
  #email-box
    = form_for resource, as: resource_name, url: registration_path(resource_name) do |form|
      = devise_error_messages!
      .input-form
        = form.email_field :email, :input_html => {:autofocus => true}, :placeholder => "What's your email address?"
        = form.submit "TAKE CONTROL"
  .subtext
    %p We're here to educate, not to spam you.
